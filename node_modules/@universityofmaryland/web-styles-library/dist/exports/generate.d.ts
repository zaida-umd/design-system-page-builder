import { minifyCSS, removeDuplicates, toKebabCase, createBlock, createClassSelector, createRules, formatValue, isPlainObject } from '../utilities/transform/css';
import { processNestedJssObjects, processWebComponentStyles, generateBaseCSS, convertToClassSelectorCss, convertToSelectorCSS, convertToCss, convertFontToCss, formatWithClassSelector, formatNestedObjects } from '../utilities/transform/jss';
import { generateTokensCSS, fromTokens, formatTokenKey, toCssObject, toString } from '../utilities/transform/variables';
export { minifyCSS, removeDuplicates, toKebabCase, createBlock, createClassSelector, createRules, formatValue, isPlainObject, };
export { processNestedJssObjects, processWebComponentStyles, generateBaseCSS, convertToClassSelectorCss, convertToSelectorCSS, convertToCss, convertFontToCss, formatWithClassSelector, formatNestedObjects, };
export { generateTokensCSS, fromTokens, formatTokenKey, toCssObject, toString, };
export interface GenerateCSSStringsResult {
    tokens: string;
    fontFaces: string;
    typography: string;
    layout: string;
    element: string;
    animation: string;
    accessibility: string;
    webComponents: string;
    base: string;
    styles: string;
}
export declare function generateCSSStrings(): Promise<GenerateCSSStringsResult>;
//# sourceMappingURL=generate.d.ts.map